export type UserType = 'student' | 'professor';

/* export interface User {
    identifier: number,
    firstname: string,
    lastname: string,
    userType: UserType,
    points: number
} */
const enum dbInfo {
    OK = 'OK',
    ERROR = 'ERROR'
}
/* export default UserType;
export default User; */
